# Local development proxy only; production must route /v1 to the real API.
API_PROXY_TARGET=http://127.0.0.1:8000
# OFF by default. Synthetic authentication fixtures, development only.
VITE_ENABLE_MOCKS=false
